{% extends 'base.html.twig' %}

{% block body %}
    <h1>Gérer mon équipe</h1>

    {{ form_start(edit_form) }}
    {{ form_widget(edit_form) }}
        <input type="submit" value="Enregistrer" class="btn btn-primary pull-right col-md-2"/>
    {{ form_end(edit_form) }}

    {{ form_start(delete_form) }}
    {{ form_end(delete_form) }}
    <div class="clearfix"></div>
{% endblock %}
